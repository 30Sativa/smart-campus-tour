VITE_API_BASE_URL=http://localhost:5000

# Auth and operations endpoints do not exist yet, so the app runs on labelled
# mock data (src/mocks/). Set to false once the backend is available.
VITE_USE_MOCK_API=true

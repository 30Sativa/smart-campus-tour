/** Class recipes of the representative area, kept out of component files (fast refresh). */

/** Panel surface: 16px radius, hairline border, a barely-there shadow. */
export const panelBase = 'rounded-2xl border border-[#e5e9f0] bg-white shadow-[0_1px_2px_rgba(15,23,42,0.04)]'

/** Clickable card: the same surface with a soft lift on hover. */
export const cardHover = 'transition-[border-color,box-shadow] duration-200 hover:border-[#cfd8e3] hover:shadow-[0_6px_20px_-12px_rgba(15,23,42,0.18)]'

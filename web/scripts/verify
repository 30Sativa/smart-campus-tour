#!/usr/bin/env bash
# Verification for web/. Exit 0 = PASS, non-zero = FAIL.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

npm ci
npm run typecheck
npm run lint
npm run test
npm run build
